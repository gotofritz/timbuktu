Reports from `make eval-defaults` land here and are committed on purpose: a
number quoted in README.md has to be re-derivable from the run that produced
it. This file only keeps the directory in git until the first sweep lands.
